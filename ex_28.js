// ternary or conditional operator
let score = 76;
let grade = (score >= 80) ? "grade A"
    : (score >= 70) ? "grade B"
        : (score >= 60) ? "grade C"
            : "grade D"
console.log(grade)    